{
  "version": "0.0.0",
  "private": true,
  "name": "sim",
  "devDependencies": {
    "coffee-script": "^1.9.2",
    "protractor": "^2.1.0",
    "phantomjs":"^1.9.17",
    "karma":"^0.12.32",
    "karma-jasmine": "~0.1.0",
    "karma-phantomjs-launcher": "~0.1",
    "karma-coffee-preprocessor": "~0.1",
    "bower": "^1.4.1"
  },
  "scripts": {
    "preupdate-webdriver": "npm install",
    "update-webdriver": "webdriver-manager update",

    "pretest": "npm install",
    "test": "karma start spec/karma.conf.coffee",

    "prekarma": "npm install",
    "karma": "karma start spec/karma.conf.coffee  --single-run",

    "preprotractor": "npm run update-webdriver",
    "protractor": "protractor spec/protractor-conf.coffee"
  }
}
